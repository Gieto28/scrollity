/**
 * input type text model being used in the create post screen, the only two input text elements are title and description
 */
interface FormCreatePostModel {
  title: string;
  description: string;
}

export default FormCreatePostModel;
